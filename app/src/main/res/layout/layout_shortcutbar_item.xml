<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="@dimen/shortcut_bar_item_width"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/shortcut_bar_item_margin"
    android:gravity="center">

    <ImageButton
        android:id="@+id/shortcut_bar_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@android:color/transparent"
        android:paddingStart="@dimen/shortcut_bar_item_padding_start"
        android:paddingTop="5dp"
        android:paddingEnd="@dimen/shortcut_bar_item_padding_end"
        android:paddingBottom="5dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_launcher_foreground"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/short_bar_app_delete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/app_delete_icon_bg"
        android:elevation="0dp"
        android:src="@drawable/app_delete_icon"
        android:translationZ="2dp"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/shortcut_bar_item" />
</android.support.constraint.ConstraintLayout>